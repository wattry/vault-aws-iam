{
  "name": "vault-aws-iam",
  "version": "1.0.0",
  "description": "Authenticates with Hashicorp vault using AWS IAM roles.",
  "main": "./dist/index.js",
  "repository": "git@github.com:wattry/vault-aws-iam.git",
  "author": "Ryan <09wattry@gmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "aws-sdk": "^2.1044.0",
    "axios": "^0.24.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.12",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  },
  "babel": {
    "plugins": [
      [
        "transform-runtime",
        {
          "regenerator": true
        }
      ]
    ],
    "presets": [
      "@babel/preset-env"
    ]
  }
}
